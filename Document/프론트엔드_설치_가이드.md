# 프론트엔드 설치 가이드

## 📋 사전 요구사항

### 필수 프로그램 설치
- **Node.js**: v18.0.0 이상 권장
- **npm**: Node.js와 함께 자동 설치됨 (v9.0.0 이상 권장)

### Node.js 설치 확인
```bash
node --version
npm --version
```

---

## 📦 설치된 모듈 목록

### 프로덕션 의존성 (dependencies)
프로젝트 실행에 필수적인 모듈들입니다.

| 모듈명 | 버전 | 용도 |
|--------|------|------|
| `react` | ^19.2.0 | React 라이브러리 (UI 프레임워크) |
| `react-dom` | ^19.2.0 | React DOM 렌더링 |
| `react-router-dom` | ^7.9.5 | React 라우팅 (페이지 이동) |
| `react-bootstrap` | ^2.10.10 | React용 Bootstrap 컴포넌트 |
| `bootstrap` | ^5.3.8 | CSS 프레임워크 |
| `recharts` | ^3.4.1 | 차트 라이브러리 (통계 그래프) |

### 개발 의존성 (devDependencies)
개발 및 빌드에 필요한 모듈들입니다.

| 모듈명 | 버전 | 용도 |
|--------|------|------|
| `vite` | ^7.2.2 | 빌드 도구 및 개발 서버 |
| `@vitejs/plugin-react-swc` | ^4.2.1 | Vite용 React 플러그인 (SWC 컴파일러) |
| `eslint` | ^9.39.1 | 코드 린터 (코드 품질 검사) |
| `@eslint/js` | ^9.39.1 | ESLint JavaScript 설정 |
| `eslint-plugin-react-hooks` | ^5.2.0 | React Hooks 린트 규칙 |
| `eslint-plugin-react-refresh` | ^0.4.24 | React Refresh 린트 규칙 |
| `globals` | ^16.5.0 | ESLint 전역 변수 설정 |
| `@types/react` | ^19.2.2 | React TypeScript 타입 정의 |
| `@types/react-dom` | ^19.2.2 | React DOM TypeScript 타입 정의 |

---

## 🚀 설치 명령문

### 1. 프로젝트 디렉토리로 이동
```bash
cd frontend
```

### 2. 모든 모듈 설치 (권장)
```bash
npm install
```

또는 짧은 명령어:
```bash
npm i
```

### 3. 프로덕션 모듈만 설치 (⚠️ 권장하지 않음)
```bash
npm install --production
```
**주의**: 이 명령어는 개발 모듈을 설치하지 않습니다. `npm run dev`나 `npm run build`가 작동하지 않습니다!

### 4. 특정 모듈 개별 설치 (필요시)
```bash
# 프로덕션 모듈
npm install react react-dom react-router-dom react-bootstrap bootstrap recharts

# 개발 모듈
npm install --save-dev vite @vitejs/plugin-react-swc eslint @eslint/js eslint-plugin-react-hooks eslint-plugin-react-refresh globals @types/react @types/react-dom
```

---

## ⚠️ 개발 모듈 설치 필수 여부

### ✅ 개발 모듈이 **필수**인 경우

다음 작업을 하려면 **반드시 개발 모듈이 필요**합니다:

1. **개발 서버 실행** (`npm run dev`)
   - `vite`가 필요합니다 (devDependencies)
   - 개발 모듈 없이는 실행 불가능

2. **프로젝트 빌드** (`npm run build`)
   - `vite build`가 필요합니다 (devDependencies)
   - 개발 모듈 없이는 빌드 불가능

3. **코드 수정 및 개발**
   - 모든 개발 도구가 devDependencies에 있습니다

### ❌ 개발 모듈이 **불필요**한 경우

다음 경우에만 개발 모듈이 필요 없습니다:

1. **이미 빌드된 파일만 배포**
   - `dist` 폴더만 있으면 됨
   - 정적 파일 서버에 올릴 때

2. **프로덕션 서버에서 실행**
   - 빌드된 파일만 필요
   - Node.js도 필요 없음

### 📌 결론: 다른 PC에서 개발하려면 **개발 모듈 필수!**

```bash
# ✅ 올바른 설치 방법 (개발 모듈 포함)
npm install

# ❌ 잘못된 방법 (개발 모듈 제외)
npm install --production  # 이렇게 하면 npm run dev가 안 됩니다!
```

---

## 🔧 개발 서버 실행

### 개발 모드 실행
```bash
npm run dev
```

기본적으로 `http://localhost:5173`에서 실행됩니다.

### 빌드 (프로덕션용)
```bash
npm run build
```

빌드된 파일은 `dist` 폴더에 생성됩니다.

### 빌드 미리보기
```bash
npm run preview
```

### 코드 린트 실행
```bash
npm run lint
```

---

## 📝 다른 PC에 설치하는 전체 과정

### ⚠️ 중요: 개발 모듈 포함 설치 필수!

다른 PC에서 **개발하려면** 반드시 개발 모듈(devDependencies)을 포함해서 설치해야 합니다.

### Windows 환경

```bash
# 1. Node.js 설치 확인 (v18 이상)
node --version
npm --version

# 2. 프로젝트 디렉토리로 이동
cd D:\java\react\workspace\goldauction\frontend

# 3. 모든 모듈 설치 (개발 모듈 포함) ✅
npm install

# 4. 개발 서버 실행 (개발 모듈이 있어야 작동)
npm run dev
```

**주의**: `npm install --production`을 사용하면 개발 모듈이 설치되지 않아 `npm run dev`가 작동하지 않습니다!

### Linux/Mac 환경

```bash
# 1. Node.js 설치 확인 (v18 이상)
node --version
npm --version

# 2. 프로젝트 디렉토리로 이동
cd /path/to/goldauction/frontend

# 3. 모든 모듈 설치 (개발 모듈 포함) ✅
npm install

# 4. 개발 서버 실행 (개발 모듈이 있어야 작동)
npm run dev
```

**주의**: `npm install --production`을 사용하면 개발 모듈이 설치되지 않아 `npm run dev`가 작동하지 않습니다!

---

## 🔄 package-lock.json이 있는 경우

`package-lock.json` 파일이 있으면 정확한 버전으로 설치됩니다:

```bash
npm ci
```

이 명령어는 `package-lock.json`의 정확한 버전으로 설치하므로 더 안정적입니다.

---

## ⚠️ 문제 해결

### node_modules 폴더 삭제 후 재설치
```bash
# Windows
rmdir /s /q node_modules
del package-lock.json
npm install

# Linux/Mac
rm -rf node_modules
rm package-lock.json
npm install
```

### 캐시 클리어 후 재설치
```bash
npm cache clean --force
npm install
```

### 특정 버전 문제 해결
```bash
# Node.js 버전 확인 (v18 이상 권장)
node --version

# npm 버전 업데이트
npm install -g npm@latest
```

---

## 📊 모듈 크기 및 용도 상세

### 주요 모듈 설명

#### React 관련
- **react**: UI 컴포넌트를 만들기 위한 핵심 라이브러리
- **react-dom**: React 컴포넌트를 DOM에 렌더링
- **react-router-dom**: SPA(Single Page Application) 라우팅 처리

#### UI 프레임워크
- **bootstrap**: 반응형 CSS 프레임워크
- **react-bootstrap**: Bootstrap을 React 컴포넌트로 사용

#### 차트 라이브러리
- **recharts**: 통계 그래프 표시 (거래 통계 페이지에서 사용)

#### 빌드 도구
- **vite**: 빠른 개발 서버 및 빌드 도구
- **@vitejs/plugin-react-swc**: SWC 컴파일러를 사용한 빠른 빌드

#### 코드 품질
- **eslint**: JavaScript/React 코드 품질 검사 도구

---

## 🎯 빠른 설치 스크립트 (복사해서 사용)

### Windows (PowerShell)
```powershell
# Node.js 버전 확인 (v18 이상 필요)
node --version
npm --version

# 프론트엔드 디렉토리로 이동
cd frontend

# 모든 모듈 설치 (개발 모듈 포함) ✅
npm install

# 개발 서버 실행 (개발 모듈이 있어야 작동)
npm run dev
```

### Linux/Mac (Bash)
```bash
# Node.js 버전 확인 (v18 이상 필요)
node --version
npm --version

# 프론트엔드 디렉토리로 이동
cd frontend

# 모든 모듈 설치 (개발 모듈 포함) ✅
npm install

# 개발 서버 실행 (개발 모듈이 있어야 작동)
npm run dev
```

**💡 핵심**: `npm install`은 자동으로 dependencies와 devDependencies를 모두 설치합니다. 개발하려면 이 방법을 사용하세요!

---

## 📌 체크리스트

다른 PC에 설치할 때 확인사항:

- [ ] Node.js v18 이상 설치 확인
- [ ] npm v9 이상 설치 확인
- [ ] `frontend` 디렉토리로 이동
- [ ] `npm install` 실행
- [ ] `node_modules` 폴더 생성 확인
- [ ] `npm run dev`로 개발 서버 실행 확인
- [ ] 브라우저에서 `http://localhost:5173` 접속 확인

---

## 🔗 참고 사항

- **package.json**: 프로젝트 의존성 및 스크립트 정의
- **package-lock.json**: 정확한 버전 정보 (자동 생성)
- **node_modules**: 설치된 모듈들이 저장되는 폴더 (git에 포함하지 않음)
- **vite.config.js**: Vite 빌드 설정 파일

---

## 💡 팁

1. **빠른 설치**: `npm ci` 사용 (package-lock.json 기반)
2. **버전 고정**: package-lock.json을 함께 배포하면 동일한 버전 설치
3. **의존성 업데이트**: `npm update`로 최신 버전으로 업데이트 가능
4. **불필요한 모듈 제거**: `npm uninstall [모듈명]`으로 제거

---

## 📞 문제 발생 시

1. **에러 메시지 확인**: 터미널에 표시된 에러 메시지 확인
2. **Node.js 버전 확인**: v18 이상인지 확인
3. **캐시 클리어**: `npm cache clean --force`
4. **재설치**: `node_modules` 삭제 후 `npm install` 재실행

